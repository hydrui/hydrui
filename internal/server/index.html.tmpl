<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/icon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Web interface for the hydrus network" />
    <meta http-equiv="Content-Security-Policy" content="{{.CSP}}" />
    <title>Hydrui</title>
    <script type="module" crossorigin src="{{.JSPath}}" integrity="{{.JSIntegrity}}"></script>
    <link rel="stylesheet" href="{{.CSSPath}}" integrity="{{.CSSIntegrity}}">
  </head>
  <body{{if .ServerMode}} data-server-mode="true"{{end}}{{if .NoAuth}} data-no-auth="true"{{end}}>
    <div id="root"></div>
  </body>
</html>
